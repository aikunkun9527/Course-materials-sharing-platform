# 基于云计算的课程资料分享平台 - 前端设计文档

## 一、前端技术栈

### 1.1 核心技术
| 技术 | 版本 | 说明 |
|------|------|------|
| React | 18.3+ | 前端框架 |
| TypeScript | 5.0+ | 类型安全 |
| Vite | 5.0+ | 构建工具 |
| React Router | 6.20+ | 路由管理 |
| Ant Design | 5.12+ | UI组件库 |
| Axios | 1.6+ | HTTP客户端 |
| Zustand/Redux | 4.4+ | 状态管理 |
| React Query | 5.0+ | 数据获取与缓存 |
| TailwindCSS | 3.4+ | CSS框架（可选） |

### 1.2 开发工具
- ESLint + Prettier：代码规范
- Husky + lint-staged：Git钩子
- Commitlint：提交信息规范

---

## 二、项目目录结构

```
frontend/
├── public/
│   ├── favicon.ico
│   ├── logo192.png
│   └── logo512.png
│
├── src/
│   ├── assets/              # 静态资源
│   │   ├── images/
│   │   ├── icons/
│   │   └── styles/
│   │       ├── global.css
│   │       ├── variables.css
│   │       └── reset.css
│   │
│   ├── components/          # 通用组件
│   │   ├── common/
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Loading.tsx
│   │   │   └── Empty.tsx
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── MainLayout.tsx
│   │   └── course/
│   │       ├── CourseCard.tsx
│   │       ├── CourseList.tsx
│   │       └── MaterialCard.tsx
│   │
│   ├── pages/              # 页面组件
│   │   ├── auth/
│   │   │   ├── LoginPage.tsx
│   │   │   ├── RegisterPage.tsx
│   │   │   └── ForgotPasswordPage.tsx
│   │   ├── home/
│   │   │   └── HomePage.tsx
│   │   ├── course/
│   │   │   ├── CourseListPage.tsx
│   │   │   ├── CourseDetailPage.tsx
│   │   │   ├── MaterialListPage.tsx
│   │   │   └── DiscussionListPage.tsx
│   │   ├── user/
│   │   │   ├── ProfilePage.tsx
│   │   │   ├── SettingsPage.tsx
│   │   │   └── MyCoursesPage.tsx
│   │   └── admin/
│   │       ├── DashboardPage.tsx
│   │       ├── UserManagePage.tsx
│   │       └── CourseManagePage.tsx
│   │
│   ├── services/           # API服务
│   │   ├── api.ts          # Axios配置
│   │   ├── auth.service.ts
│   │   ├── user.service.ts
│   │   ├── course.service.ts
│   │   ├── material.service.ts
│   │   └── discussion.service.ts
│   │
│   ├── hooks/              # 自定义Hooks
│   │   ├── useAuth.ts
│   │   ├── usePagination.ts
│   │   ├── useUpload.ts
│   │   └── useDebounce.ts
│   │
│   ├── store/              # 状态管理
│   │   ├── authStore.ts
│   │   ├── courseStore.ts
│   │   └── userStore.ts
│   │
│   ├── types/              # TypeScript类型定义
│   │   ├── auth.types.ts
│   │   ├── user.types.ts
│   │   ├── course.types.ts
│   │   └── common.types.ts
│   │
│   ├── utils/              # 工具函数
│   │   ├── request.ts      # HTTP请求封装
│   │   ├── storage.ts      # 本地存储
│   │   ├── format.ts       # 格式化
│   │   └── validate.ts     # 验证
│   │
│   ├── constants/          # 常量定义
│   │   ├── routes.ts
│   │   ├── config.ts
│   │   └── enums.ts
│   │
│   ├── App.tsx             # 根组件
│   ├── main.tsx            # 入口文件
│   └── vite-env.d.ts
│
├── .env.development        # 开发环境变量
├── .env.production         # 生产环境变量
├── .eslintrc.cjs
├── .prettierrc
├── tsconfig.json
├── vite.config.ts
└── package.json
```

---

## 三、页面设计

### 3.1 页面路由结构

```typescript
// routes.ts
const routes = [
  {
    path: '/login',
    component: LoginPage,
    meta: { requiresAuth: false, title: '登录' }
  },
  {
    path: '/register',
    component: RegisterPage,
    meta: { requiresAuth: false, title: '注册' }
  },
  {
    path: '/',
    component: MainLayout,
    meta: { requiresAuth: true },
    children: [
      {
        path: '',
        component: HomePage,
        meta: { title: '首页' }
      },
      {
        path: 'courses',
        component: CourseListPage,
        meta: { title: '课程列表' }
      },
      {
        path: 'courses/:id',
        component: CourseDetailPage,
        meta: { title: '课程详情' }
      },
      {
        path: 'courses/:id/materials',
        component: MaterialListPage,
        meta: { title: '课程资料' }
      },
      {
        path: 'courses/:id/discussions',
        component: DiscussionListPage,
        meta: { title: '课程讨论' }
      },
      {
        path: 'profile',
        component: ProfilePage,
        meta: { title: '个人中心' }
      },
      {
        path: 'settings',
        component: SettingsPage,
        meta: { title: '设置' }
      },
      {
        path: 'my-courses',
        component: MyCoursesPage,
        meta: { title: '我的课程' }
      }
    ]
  },
  {
    path: '/admin',
    component: AdminLayout,
    meta: { requiresAuth: true, requiresAdmin: true },
    children: [
      {
        path: '',
        component: DashboardPage,
        meta: { title: '管理后台' }
      },
      {
        path: 'users',
        component: UserManagePage,
        meta: { title: '用户管理' }
      },
      {
        path: 'courses',
        component: CourseManagePage,
        meta: { title: '课程管理' }
      }
    ]
  }
];
```

---

### 3.2 登录页面 (LoginPage)

#### 页面布局
```
┌────────────────────────────────────────┐
│                                        │
│           [Logo]                       │
│     课程资料分享平台                    │
│                                        │
│   ┌────────────────────────────┐      │
│   │  邮箱                      │      │
│   └────────────────────────────┘      │
│                                        │
│   ┌────────────────────────────┐      │
│   │  密码               [忘记] │      │
│   └────────────────────────────┘      │
│                                        │
│   ┌────────────────────────────┐      │
│   │      [ 登 录 ]             │      │
│   └────────────────────────────┘      │
│                                        │
│         没有账号？ [立即注册]          │
│                                        │
└────────────────────────────────────────┘
```

#### 核心组件
```tsx
// pages/auth/LoginPage.tsx
import { Form, Input, Button, message } from 'antd';
import { useAuth } from '@/hooks/useAuth';
import { useNavigate } from 'react-router-dom';

const LoginPage = () => {
  const { login } = useAuth();
  const navigate = useNavigate();
  const [form] = Form.useForm();

  const onFinish = async (values: any) => {
    try {
      await login(values.email, values.password);
      message.success('登录成功');
      navigate('/');
    } catch (error) {
      message.error('登录失败，请检查邮箱和密码');
    }
  };

  return (
    <div className="login-container">
      <div className="login-box">
        <div className="logo">课程资料分享平台</div>
        <Form form={form} onFinish={onFinish}>
          <Form.Item name="email" rules={[{ required: true }]}>
            <Input placeholder="邮箱" />
          </Form.Item>
          <Form.Item name="password" rules={[{ required: true }]}>
            <Input.Password placeholder="密码" />
          </Form.Item>
          <Button type="primary" htmlType="submit" block>
            登录
          </Button>
        </Form>
        <div className="register-link">
          没有账号？<a href="/register">立即注册</a>
        </div>
      </div>
    </div>
  );
};
```

---

### 3.3 注册页面 (RegisterPage)

#### 页面布局
```
┌────────────────────────────────────────┐
│           [Logo]                       │
│     创建新账号                          │
│                                        │
│   邮箱                                  │
│   ┌────────────────────────────┐      │
│   └────────────────────────────┘      │
│                                        │
│   用户名                                │
│   ┌────────────────────────────┐      │
│   └────────────────────────────┘      │
│                                        │
│   密码                                  │
│   ┌────────────────────────────┐      │
│   └────────────────────────────┘      │
│                                        │
│   确认密码                              │
│   ┌────────────────────────────┐      │
│   └────────────────────────────┘      │
│                                        │
│   （注册后默认为学生角色）              │
│                                        │
│   ┌────────────────────────────┐      │
│   │      [ 注 册 ]             │      │
│   └────────────────────────────┘      │
│                                        │
│         已有账号？ [立即登录]          │
└────────────────────────────────────────┘
```

---

### 3.4 首页 (HomePage)

#### 页面布局
```
┌────────────────────────────────────────────────────────────┐
│  [Logo]  首页  课程  我的课程           [🔍]  [头像]      │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌────────────────────────────────────────────────┐       │
│  │  欢迎来到课程资料分享平台                        │       │
│  │  探索课程，分享知识，共同成长                    │       │
│  └────────────────────────────────────────────────┘       │
│                                                            │
│  热门课程                                                  │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐                │
│  │ [封面]│  │ [封面]│  │ [封面]│  │ [封面]│                │
│  │ 课程1 │  │ 课程2 │  │ 课程3 │  │ 课程4 │                │
│  │ 120人 │  │ 85人  │  │ 200人 │  │ 95人  │                │
│  └──────┘  └──────┘  └──────┘  └──────┘                │
│                                                            │
│  最新资料                                                  │
│  ┌───────────────────────────────────────────────┐        │
│  │ • 算法导论第三章.pdf         王教授  2天前     │        │
│  │ • 机器学习实战笔记.docx      李华    3天前     │        │
│  │ • 数据结构习题解答.pdf       张老师  5天前     │        │
│  └───────────────────────────────────────────────┘        │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

### 3.5 课程列表页 (CourseListPage)

#### 页面布局
```
┌────────────────────────────────────────────────────────────┐
│  [Logo]  首页  课程  我的课程           [🔍]  [头像]      │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  筛选: [全部] [计算机] [数学] [物理] [文学]                │
│                                                            │
│  排序: [最新] [最热] [成员最多]                            │
│                                                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │   [封面]    │  │   [封面]    │  │   [封面]    │        │
│  │ 数据结构与算法│  │ 机器学习基础│  │ 操作系统原理│        │
│  │ 王教授      │  │ 李教授      │  │ 张教授      │        │
│  │ 120成员    │  │ 85成员      │  │ 200成员     │        │
│  │ ⭐ 4.8     │  │ ⭐ 4.9     │  │ ⭐ 4.7      │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
│                                                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │   [封面]    │  │   [封面]    │  │   [封面]    │        │
│  │ 编译原理    │  │ 计算机网络  │  │ 数据库原理  │        │
│  │ ...         │  │ ...         │  │ ...         │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
│                                                            │
│  [< 上一页]  1 2 3 ... 10 [下一页 >]                      │
└────────────────────────────────────────────────────────────┘
```

#### 核心组件
```tsx
// pages/course/CourseListPage.tsx
import { useState, useEffect } from 'react';
import { Card, Row, Col, Pagination, Select, Input } from 'antd';
import { useCourse } from '@/hooks/useCourse';
import CourseCard from '@/components/course/CourseCard';

const CourseListPage = () => {
  const [filters, setFilters] = useState({
    category: 'all',
    keyword: '',
    sort: 'latest',
    page: 1,
    limit: 12
  });

  const { data, isLoading } = useCourse(filters);

  return (
    <div className="course-list-page">
      {/* 搜索和筛选 */}
      <div className="filters">
        <Input.Search
          placeholder="搜索课程"
          onSearch={(keyword) => setFilters({ ...filters, keyword })}
        />
        <Select
          defaultValue="all"
          onChange={(category) => setFilters({ ...filters, category })}
        >
          <Option value="all">全部分类</Option>
          <Option value="computer">计算机</Option>
          <Option value="math">数学</Option>
        </Select>
        <Select
          defaultValue="latest"
          onChange={(sort) => setFilters({ ...filters, sort })}
        >
          <Option value="latest">最新</Option>
          <Option value="hottest">最热</Option>
        </Select>
      </div>

      {/* 课程列表 */}
      <Row gutter={[16, 16]}>
        {data?.courses.map((course) => (
          <Col xs={24} sm={12} md={8} lg={6} key={course.id}>
            <CourseCard course={course} />
          </Col>
        ))}
      </Row>

      {/* 分页 */}
      <Pagination
        current={filters.page}
        total={data?.pagination.total || 0}
        pageSize={filters.limit}
        onChange={(page) => setFilters({ ...filters, page })}
      />
    </div>
  );
};
```

---

### 3.6 课程详情页 (CourseDetailPage)

#### 页面布局
```
┌────────────────────────────────────────────────────────────┐
│  [Logo]  首页  课程  我的课程           [🔍]  [头像]      │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌────────────────────────────────────────────────┐       │
│  │ [课程封面]                                      │       │
│  │                                                │       │
│  │ 数据结构与算法                      [加入课程] │       │
│  │ 王教授 · 计算机系                               │       │
│  │                                                │       │
│  │ 学习基础数据结构和算法设计，包括链表、树、图等  │       │
│  │ 数据结构，以及排序、搜索等算法。                │       │
│  │                                                │       │
│  │ 分类: 计算机    成员: 120/200    资料: 15      │       │
│  └────────────────────────────────────────────────┘       │
│                                                            │
│  ┌─────────────┐  ┌─────────────┐                        │
│  │   📁 资料    │  │   💬 讨论   │                        │
│  │             │  │             │                        │
│  │ [15份资料]  │  │ [8个讨论]   │                        │
│  │             │  │             │                        │
│  │ • 算法导论  │  │ • 快速排序  │                        │
│  │   第三章    │  │   时间复杂度│                        │
│  │             │  │             │                        │
│  │ • 数据结构  │  │ • 二叉树    │                        │
│  │   习题解答  │  │   遍历问题  │                        │
│  └─────────────┘  └─────────────┘                        │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

### 3.7 课程资料页 (MaterialListPage)

#### 页面布局
```
┌────────────────────────────────────────────────────────────┐
│ [返回] 数据结构与算法 - 资料           [上传资料]          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  筛选: [全部] [PDF] [PPT] [DOC] [视频]                     │
│  排序: [最新] [最热] [最多下载]                            │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │  📄 算法导论第三章.pdf               王教授        │     │
│  │     排序算法详解                                 │     │
│  │     5.0 MB · PDF · 45次下载 · 2天前              │     │
│  │                             [下载] [预览]         │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │  📄 数据结构习题解答.pdf               张老师      │     │
│  │     第一章至第五章习题详细解答                   │     │
│  │     3.2 MB · PDF · 28次下载 · 5天前              │     │
│  │                             [下载] [预览]         │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │  🎬 算法动画演示.mp4                  李华         │     │
│  │     快速排序、归并排序可视化演示                 │     │
│  │     125.0 MB · MP4 · 15次下载 · 1周前           │     │
│  │                             [下载] [预览]         │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

#### 上传资料模态框
```
┌─────────────────────────────────────┐
│          上传资料                    │
├─────────────────────────────────────┤
│  标题:                               │
│  ┌─────────────────────────────┐    │
│  └─────────────────────────────┘    │
│                                     │
│  描述:                               │
│  ┌─────────────────────────────┐    │
│  └─────────────────────────────┘    │
│                                     │
│  文件:                               │
│  ┌─────────────────────────────┐    │
│  │   [选择文件]                │    │
│  │   或拖拽文件到此区域         │    │
│  └─────────────────────────────┘    │
│                                     │
│  进度: ████████████ 100%            │
│                                     │
│           [取消]  [上传]            │
└─────────────────────────────────────┘
```

---

### 3.8 课程讨论页 (DiscussionListPage)

#### 页面布局
```
┌────────────────────────────────────────────────────────────┐
│ [返回] 数据结构与算法 - 讨论           [发布讨论]          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  置顶:                                                     │
│  ┌──────────────────────────────────────────────────┐     │
│  │ 📌 课程学习资源汇总                 王教授        │     │
│  │     本课程所有学习资源整理，持续更新中...        │     │
│  │     👍 45  💬 12  👁️ 1280        [置顶]        │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  全部讨论:                                                 │
│  ┌──────────────────────────────────────────────────┐     │
│  │ 如何理解快速排序的时间复杂度？         张三       │     │
│  │     在分析快速排序时，为什么平均是O(nlogn)？    │     │
│  │     👍 15  💬 8  👁️ 128           2天前         │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │ 二叉树的非递归遍历怎么理解？           李四       │     │
│  │     用栈实现的非递归遍历有些地方不太懂...        │     │
│  │     👍 8  💬 5  👁️ 76             3天前         │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  [< 上一页]  1 2 3 [下一页 >]                             │
└────────────────────────────────────────────────────────────┘
```

#### 讨论详情
```
┌────────────────────────────────────────────────────────────┐
│ [返回] 如何理解快速排序的时间复杂度？                      │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  作者: 张三 · 2天前                                        │
│                                                            │
│  在分析快速排序时，为什么平均是O(nlogn)？                  │
│  最坏情况下的时间复杂度又是多少？                          │
│                                                            │
│  ─────────────────────────────────────────────────        │
│                                                            │
│  王教授 · 2天前                                            │
│  快速排序的平均时间复杂度确实是O(nlogn)，这是因为每次分区   │
│  大约将数组分成两半，递归深度为logn，每层处理n个元素...    │
│  👍 5  [回复]                                              │
│                                                            │
│  ┌───────────────────────────────────────┐                │
│  │ 张三 · 2天前                           │                │
│  │ 感谢教授的解答，我明白了！              │                │
│  │ 👍 0                                   │                │
│  └───────────────────────────────────────┘                │
│                                                            │
│  ─────────────────────────────────────────────────        │
│                                                            │
│  [添加评论...]                    [💜 点赞] [💬 回复]      │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

### 3.9 个人中心页 (ProfilePage)

#### 页面布局
```
┌────────────────────────────────────────────────────────────┐
│  [Logo]  首页  课程  我的课程           [🔍]  [头像]      │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────┐  个人信息                  [编辑]       │
│  │    [头像]    │  张三                                                 │
│  │              │  student@example.com                                  │
│  │              │  计算机科学专业学生                                    │
│  │              │  加入时间: 2025-12-01                                 │
│  └──────────────┘                                                        │
│                                                                          │
│  统计信息                                                                │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐                        │
│  │   5    │  │   120  │  │   85   │  │   45   │                        │
│  │  已加入 │  │  资料数 │  │  下载数 │  │  讨论数 │                        │
│  └────────┘  └────────┘  └────────┘  └────────┘                        │
│                                                                          │
│  我的课程                                                                │
│  ┌───────────────────────────────────────────────┐                      │
│  │ [封面] 数据结构与算法             王教授        │                      │
│  │        120成员 · 15份资料 · 进入课程 →         │                      │
│  └───────────────────────────────────────────────┘                      │
│                                                                          │
│  ┌───────────────────────────────────────────────┐                      │
│  │ [封面] 机器学习基础               李教授        │                      │
│  │        85成员 · 12份资料 · 进入课程 →          │                      │
│  └───────────────────────────────────────────────┘                      │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

---

### 3.10 设置页 (SettingsPage)

#### 页面布局
```
┌────────────────────────────────────────────────────────────┐
│  设置                                                        │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌────────────────────────────────────────────────┐       │
│  │ 基本信息                                       │       │
│  │                                                │       │
│  │  用户名: [张三                    ]            │       │
│  │  邮箱:   [student@example.com     ] (不可修改) │       │
│  │  个人简介:                              │       │
│  │  ┌────────────────────────────────────┐      │       │
│  │  │ 热爱学习，热爱编程                 │      │       │
│  │  └────────────────────────────────────┘      │       │
│  │                                                │       │
│  │                        [保存修改]              │       │
│  └────────────────────────────────────────────────┘       │
│                                                            │
│  ┌────────────────────────────────────────────────┐       │
│  │ 修改密码                                       │       │
│  │                                                │       │
│  │  当前密码: [********************               │       │
│  │  新密码:   [********************               │       │
│  │  确认密码: [********************               │       │
│  │                                                │       │
│  │                        [修改密码]              │       │
│  └────────────────────────────────────────────────┘       │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

### 3.11 管理后台页 (DashboardPage)

#### 页面布局
```
┌────────────────────────────────────────────────────────────┐
│  管理后台                                     [管理员] [退出]│
├──────────┬─────────────────────────────────────────────────┤
│          │                                                 │
│  仪表盘  │  系统统计                                       │
│  用户管理 │  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  课程管理 │  │  1500   │  │   85    │  │  1250   │    │
│  资料管理 │  │  总用户  │  │  总课程  │  │  总资料  │    │
│  讨论管理 │  └──────────┘  └──────────┘  └──────────┘    │
│          │                                                 │
│          │  用户增长趋势                                   │
│          │  ┌─────────────────────────────────────────┐   │
│          │  │     █                                   │   │
│          │  │    █ █                                  │   │
│          │  │   █   █                                 │   │
│          │  │  █     █                                │   │
│          │  └─────────────────────────────────────────┘   │
│          │  12月  12月  12月  12月  12月                   │
│          │   1日   5日   10日  15日  20日                 │
│          │                                                 │
│          │  最新活动                                       │
│          │  • 张三 加入了"数据结构与算法"                  │
│          │  • 李华 上传了"算法导论第三章.pdf"              │
│          │  • 王教授 创建了新课程"机器学习进阶"            │
│          │                                                 │
└──────────┴─────────────────────────────────────────────────┘
```

---

## 四、通用组件设计

### 4.1 主布局组件 (MainLayout)

```tsx
// components/layout/MainLayout.tsx
import { Outlet, useNavigate } from 'react-router-dom';
import { Layout, Menu, Avatar, Dropdown } from 'antd';
import { UserOutlined, LogoutOutlined } from '@ant-design/icons';
import { useAuth } from '@/hooks/useAuth';

const { Header, Content, Footer } = Layout;

const MainLayout = () => {
  const { user, logout } = useAuth();
  const navigate = useNavigate();

  const menuItems = [
    { key: '/', label: '首页' },
    { key: '/courses', label: '课程' },
    { key: '/my-courses', label: '我的课程' },
  ];

  const userMenuItems = [
    {
      key: 'profile',
      icon: <UserOutlined />,
      label: '个人中心',
      onClick: () => navigate('/profile'),
    },
    {
      key: 'logout',
      icon: <LogoutOutlined />,
      label: '退出登录',
      onClick: logout,
    },
  ];

  return (
    <Layout className="main-layout">
      <Header>
        <div className="logo" onClick={() => navigate('/')}>
          课程资料分享平台
        </div>
        <Menu mode="horizontal" items={menuItems} />
        <Dropdown menu={{ items: userMenuItems }}>
          <Avatar icon={<UserOutlined />} src={user?.avatar_url} />
        </Dropdown>
      </Header>
      <Content className="content">
        <Outlet />
      </Content>
      <Footer>
        课程资料分享平台 ©2025 Created by Ant Design
      </Footer>
    </Layout>
  );
};
```

---

### 4.2 课程卡片组件 (CourseCard)

```tsx
// components/course/CourseCard.tsx
import { Card, Tag, Avatar } from 'antd';
import { UserOutlined } from '@ant-design/icons';
import { useNavigate } from 'react-router-dom';
import type { Course } from '@/types/course.types';

interface CourseCardProps {
  course: Course;
}

const CourseCard = ({ course }: CourseCardProps) => {
  const navigate = useNavigate();

  return (
    <Card
      hoverable
      cover={<img alt={course.title} src={course.cover_url} />}
      onClick={() => navigate(`/courses/${course.id}`)}
      className="course-card"
    >
      <Card.Meta
        title={<div className="course-title">{course.title}</div>}
        description={
          <div className="course-info">
            <div className="creator">
              <Avatar icon={<UserOutlined />} src={course.creator.avatar_url} />
              <span>{course.creator.username}</span>
            </div>
            <div className="stats">
              <Tag color="blue">{course.category}</Tag>
              <span>{course.current_students} 成员</span>
            </div>
          </div>
        }
      />
    </Card>
  );
};

export default CourseCard;
```

---

### 4.3 资料卡片组件 (MaterialCard)

```tsx
// components/course/MaterialCard.tsx
import { Card, Button, Tag, Space } from 'antd';
import { DownloadOutlined, EyeOutlined } from '@ant-design/icons';
import type { Material } from '@/types/course.types';

interface MaterialCardProps {
  material: Material;
  onDownload: (material: Material) => void;
  onPreview: (material: Material) => void;
}

const MaterialCard = ({ material, onDownload, onPreview }: MaterialCardProps) => {
  const formatFileSize = (bytes: number) => {
    if (bytes < 1024 * 1024) {
      return `${(bytes / 1024).toFixed(1)} KB`;
    }
    return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
  };

  return (
    <Card className="material-card">
      <div className="material-header">
        <div className="material-icon">
          {material.file_extension === 'pdf' && '📄'}
          {material.file_extension === 'pptx' && '📊'}
          {material.file_extension === 'mp4' && '🎬'}
        </div>
        <div className="material-info">
          <div className="material-title">{material.title}</div>
          <div className="material-description">{material.description}</div>
        </div>
      </div>
      <div className="material-meta">
        <Tag>{material.file_extension.toUpperCase()}</Tag>
        <span>{formatFileSize(material.file_size)}</span>
        <span>{material.download_count} 次下载</span>
      </div>
      <div className="material-footer">
        <Space>
          <Button icon={<DownloadOutlined />} onClick={() => onDownload(material)}>
            下载
          </Button>
          <Button icon={<EyeOutlined />} onClick={() => onPreview(material)}>
            预览
          </Button>
        </Space>
      </div>
    </Card>
  );
};

export default MaterialCard;
```

---

### 4.4 文件上传组件 (FileUpload)

```tsx
// components/common/FileUpload.tsx
import { Upload, message, Progress } from 'antd';
import { InboxOutlined } from '@ant-design/icons';
import { useState } from 'react';
import { useUpload } from '@/hooks/useUpload';

const { Dragger } = Upload;

interface FileUploadProps {
  courseId: number;
  onSuccess: (material: any) => void;
}

const FileUpload = ({ courseId, onSuccess }: FileUploadProps) => {
  const [uploading, setUploading] = useState(false);
  const [progress, setProgress] = useState(0);
  const { uploadToOSS } = useUpload();

  const handleUpload = async (file: File) => {
    setUploading(true);

    try {
      // 1. 获取OSS签名
      const signature = await uploadToOSS.getSignature(file);

      // 2. 上传到OSS
      await uploadToOSS.uploadToOSS(file, signature, (percent) => {
        setProgress(percent);
      });

      // 3. 通知后端创建记录
      const material = await uploadToOSS.createMaterialRecord({
        course_id: courseId,
        file_name: file.name,
        file_size: file.size,
        file_url: signature.url,
      });

      message.success('上传成功');
      onSuccess(material);
    } catch (error) {
      message.error('上传失败');
    } finally {
      setUploading(false);
      setProgress(0);
    }
  };

  return (
    <Dragger
      accept=".pdf,.doc,.docx,.ppt,.pptx,.mp4,.zip"
      beforeUpload={handleUpload}
      disabled={uploading}
      showUploadList={false}
    >
      <p className="ant-upload-drag-icon">
        <InboxOutlined />
      </p>
      <p className="ant-upload-text">点击或拖拽文件到此区域上传</p>
      <p className="ant-upload-hint">
        支持 PDF、DOC、PPT、MP4 等格式，单个文件不超过 100MB
      </p>
      {uploading && <Progress percent={progress} />}
    </Dragger>
  );
};

export default FileUpload;
```

---

## 五、状态管理

### 5.1 使用 Zustand 进行状态管理

```tsx
// store/authStore.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import type { User } from '@/types/user.types';

interface AuthState {
  user: User | null;
  token: string | null;
  setAuth: (user: User, token: string) => void;
  clearAuth: () => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      token: null,
      setAuth: (user, token) => set({ user, token }),
      clearAuth: () => set({ user: null, token: null }),
    }),
    {
      name: 'auth-storage',
    }
  )
);
```

### 5.2 使用 React Query 进行数据获取

```tsx
// hooks/useCourse.ts
import { useQuery, useMutation } from '@tanstack/react-query';
import { courseService } from '@/services/course.service';

export const useCourse = (filters: any) => {
  return useQuery({
    queryKey: ['courses', filters],
    queryFn: () => courseService.getCourses(filters),
  });
};

export const useJoinCourse = () => {
  return useMutation({
    mutationFn: (courseId: number) => courseService.joinCourse(courseId),
    onSuccess: () => {
      // 成功后刷新数据
      queryClient.invalidateQueries({ queryKey: ['courses'] });
    },
  });
};
```

---

## 六、样式设计

### 6.1 使用 CSS Modules

```css
/* pages/course/CourseListPage.module.css */
.container {
  padding: 24px;
  max-width: 1200px;
  margin: 0 auto;
}

.filters {
  display: flex;
  gap: 16px;
  margin-bottom: 24px;
  flex-wrap: wrap;
}

.courseGrid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 24px;
  margin-bottom: 24px;
}

.pagination {
  display: flex;
  justify-content: center;
  margin-top: 32px;
}
```

### 6.2 使用 TailwindCSS（可选）

```tsx
<div className="min-h-screen bg-gray-50">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {/* 课程卡片 */}
    </div>
  </div>
</div>
```

---

## 七、响应式设计

### 7.1 断点设置

```css
/* breakpoints.css */
:root {
  --breakpoint-sm: 640px;
  --breakpoint-md: 768px;
  --breakpoint-lg: 1024px;
  --breakpoint-xl: 1280px;
}
```

### 7.2 响应式布局

```tsx
<Col xs={24} sm={12} md={8} lg={6}>
  <CourseCard />
</Col>
```

---

## 八、用户体验优化

### 8.1 加载状态

```tsx
const { data, isLoading, error } = useCourse(filters);

if (isLoading) return <Loading />;
if (error) return <Error message="加载失败" />;
```

### 8.2 错误处理

```tsx
const Error = ({ message, onRetry }) => (
  <div className="error-container">
    <p>{message}</p>
    <Button onClick={onRetry}>重试</Button>
  </div>
);
```

### 8.3 空状态

```tsx
if (!data || data.courses.length === 0) {
  return <Empty description="暂无课程" />;
}
```

---

*文档版本: v1.0*
*创建日期: 2025-12-26*
